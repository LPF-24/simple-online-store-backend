spring:
  application:
    name:

  datasource:
  # PostgreSQL is running in a Docker container under the hostname 'db'
    url: jdbc:postgresql:// # Connect to the database inside the Docker container
    username:
    password:

  jpa:
    hibernate:
      ddl-auto:
    show-sql:
    properties:
      hibernate:
        format_sql:

admin:
  registration:
    code:

logging:
  level:
    root:
    org:
      springframework:
        security:
        orm.jpa:
        jdbc.core:

jwt_secret: